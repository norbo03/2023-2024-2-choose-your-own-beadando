import { EncryptPipePipe } from './encrypt.pipe';

describe('EncryptPipePipe', () => {
  it('create an instance', () => {
    const pipe = new EncryptPipePipe();
    expect(pipe).toBeTruthy();
  });
});
